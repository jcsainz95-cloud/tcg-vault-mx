# security/secretos-exigidos.txt — GENERADO. No editar a mano.
#   Regenerar: ./scripts/gen-published-secrets-manifest.sh
#
# Los secretos que los docker-compose*.yml EXIGEN con ${VAR:?}. No es una
# lista escrita a mano: se deriva del compose, así que el secreto que alguien
# añada mañana entra solo. Viaja en la imagen del backend para que
# `secrets-preflight.sh assert` tenga BLANCO también en Railway, donde no hay
# ficheros de compose.
POSTGRES_PASSWORD
RESEND_API_KEY
S3_SECRET_ACCESS_KEY
STAGING_JWT_ACCESS_SECRET
STAGING_JWT_REFRESH_SECRET
STAGING_MINIO_ROOT_PASSWORD
STAGING_PII_ENCRYPTION_KEY
STAGING_PII_HMAC_KEY
STAGING_POSTGRES_PASSWORD
STAGING_SEED_ADMIN_PASSWORD
STAGING_SEED_OPERATOR_PASSWORD
STRIPE_TEST_SECRET_KEY
STRIPE_TEST_WEBHOOK_SECRET
STRIPE_WEBHOOK_SECRET
