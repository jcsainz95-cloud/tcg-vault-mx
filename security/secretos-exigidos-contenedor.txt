# security/secretos-exigidos-contenedor.txt — GENERADO. No editar a mano.
#   Regenerar: ./scripts/gen-published-secrets-manifest.sh
#
# Los nombres tal como los ve el proceso DENTRO del contenedor, que NO son los
# del host. En `JWT_ACCESS_SECRET: ${STAGING_JWT_ACCESS_SECRET:?…}` el de la
# izquierda es el del contenedor y el de la derecha el del host. Meter la lista
# del host en la imagen hizo que 12 de 15 «faltaran», el entrypoint abortara y
# el backend muriera sin emitir nada (run 34531002011).
DATABASE_URL
JWT_ACCESS_SECRET
JWT_REFRESH_SECRET
PII_ENCRYPTION_KEY
PII_HMAC_KEY
RESEND_API_KEY
S3_SECRET_ACCESS_KEY
SEED_ADMIN_PASSWORD
SEED_OPERATOR_PASSWORD
STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET
